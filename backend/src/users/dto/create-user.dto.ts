export class CreateUserDto {
  name: string;
  email: string;
  field?: string;
  postcode?: string;
  role?: string;
}
//Javascript class
//response from client
//define what user info needs to be sent for request
